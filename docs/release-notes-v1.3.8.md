# Tomo v1.3.8 — Custom Roll Amount

Adds a custom 1–20 pick count to Randomize while preserving the existing single-result path at 1.

Multi-pick rolls reuse Tomo's existing cached candidate/reroll architecture, render unique picks in a mobile-friendly batch, retain per-title AniList/Maybe Later/Not Tonight actions, and remember the selected amount locally. The PWA shell cache is bumped to `tomo-shell-v1.3.8-batch-roll-20`.

Static wiring review completed. Physical installed-PWA QA remains required before the feature is marked Done.
