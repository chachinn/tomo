# Regression scope

The custom roll amount feature is scoped to Randomize. Navigation, AniList OAuth, library sync/write-back, Discover, and existing filter semantics are not intentionally changed. Existing single-result behavior remains the fallback when the selected amount is 1.
